name: 更新資料

on:
  schedule:
    - cron: '0 9 * * *' 
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: 取得原始碼
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: 安裝 Python（含快取）
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: "pip"
          cache-dependency-path: "tools/my-hw-list/requirements.txt"

      - name: 安裝套件
        working-directory: tools/my-hw-list
        run: pip install -r requirements.txt

      - name: 執行更新腳本
        working-directory: tools/my-hw-list
        env:
          PORTAL_USERNAME: ${{ secrets.PORTAL_USERNAME }}
          PORTAL_PASSWORD: ${{ secrets.PORTAL_PASSWORD }}
        run: python homework_crawler.py

      # --- Summary 美化 ---
      - name: 產生更新摘要（Summary）
        if: always()
        run: |
          {
            echo "# 📊 家課資料更新報告"
            echo ""
            echo "⏱️ **執行時間：** $(date)"
            echo ""
            
            echo "## 🧭 更新模式"
            if [ "${{ github.event.inputs.force_full_update }}" = "true" ]; then
              echo "- 🚀 **強制完整更新**"
            else
              echo "- 📅 **日常增量更新**"
            fi
            echo ""
            
            echo "## 📦 更新結果"
            if [ "${{ steps.crawler.outputs.data_updated }}" = "true" ]; then
              echo "### ✅ 資料更新成功"
              echo ""
              echo "| 項目 | 數值 |"
              echo "|------|------|"
              echo "| 🗂️ 資料筆數 | **${{ steps.crawler.outputs.record_count }} 筆** |"
              echo "| 🔄 觸發方式 | \`${{ github.event_name }}\` |"
              
              if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
                echo "| 👤 手動觸發者 | \`${{ github.actor }}\` |"
              fi
              
              if [ "${{ steps.git-check.outputs.has_changes }}" = "true" ]; then
                echo "| 🚀 部署狀態 | **已自動觸發部署** |"
              else
                echo "| ℹ️ 部署狀態 | 無資料變更，未進行部署 |"
              fi
              
            else
              echo "### ❌ 資料更新失敗"
              echo ""
              echo "> ⚠️ 請檢查：GitHub Secrets、網路連線、登入資訊是否設定正確"
            fi
            
          } >> $GITHUB_STEP_SUMMARY


      # --- Commit 若有變更才進行 ---
      - name: 提交更新
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add tools/my-hw-list/homework_data.json
          git add tools/my-hw-list/last_update.json

          if git diff --cached --quiet; then
            echo "⚠️ 無變更可提交，略過 commit。"
            exit 0
          fi

          git commit -m "自動更新資料"
          git push
