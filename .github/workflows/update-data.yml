name: Update Homework Data

on:
  schedule:
    - cron: "0 9 * * *" # 每天 UTC 09:00（香港時間 17:00）觸發
  workflow_dispatch:
    inputs:
      force_full_update:
        description: "強制完整更新（忽略增量更新邏輯）"
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  actions: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"
          cache-dependency-path: "hw-list/requirements.txt"

      - name: Install Dependencies
        working-directory: hw-list
        run: |
          python -m pip install --upgrade pip
          if [ -f "requirements.txt" ]; then
            pip install -r requirements.txt
          fi

      - name: Run Crawler
        id: crawler
        working-directory: hw-list
        env:
          PORTAL_USERNAME: ${{ secrets.PORTAL_USERNAME }}
          PORTAL_PASSWORD: ${{ secrets.PORTAL_PASSWORD }}
          FORCE_FULL_UPDATE: ${{ github.event.inputs.force_full_update == 'true' && 'true' || 'false' }}
        run: |
          if [ ! -f "homework_crawler.py" ]; then
            echo "❌ 未找到 homework_crawler.py"
            echo "data_updated=false" >> $GITHUB_OUTPUT
            exit 1
          fi

          if [ "$FORCE_FULL_UPDATE" = "true" ]; then
            echo "🚀 執行強制完整更新"
          else
            echo "📅 執行日常增量更新"
          fi

          python homework_crawler.py

          if [ -f "homework_data.json" ]; then
            if python -c "import json; json.load(open('homework_data.json'))" 2>/dev/null; then
              RECORD_COUNT=$(python -c "import json; print(len(json.load(open('homework_data.json'))))")
              echo "data_updated=true" >> $GITHUB_OUTPUT
              echo "record_count=$RECORD_COUNT" >> $GITHUB_OUTPUT
              echo "✅ 數據文件驗證通過，共 $RECORD_COUNT 條記錄"
            else
              echo "❌ homework_data.json 格式錯誤"
              echo "data_updated=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "❌ 未生成 homework_data.json"
            echo "data_updated=false" >> $GITHUB_OUTPUT
          fi

      - name: Configure Git
        if: steps.crawler.outputs.data_updated == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # 只暫存 homework_data.json，檢查是否有真實資料變更
      - name: Check for data changes
        id: git-check
        if: steps.crawler.outputs.data_updated == 'true'
        working-directory: hw-list
        run: |
          git add homework_data.json
          if git diff --staged --quiet; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "⚠️ homework_data.json 無變更，跳過更新"
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "✅ homework_data.json 有變更"
          fi

      # 僅當資料檔真正改變時，才寫入新的 last_update.json 並提交
      - name: Create Last_Update Data
        if: steps.git-check.outputs.has_changes == 'true'
        working-directory: hw-list
        run: |
          cat > last_update.json << EOF
          {
            "last_updated": "$(date -Iseconds)",
            "run_id": "${{ github.run_id }}",
            "trigger": "${{ github.event_name }}",
            "records": ${{ steps.crawler.outputs.record_count }}
          }
          EOF
          echo "✅ 已建立新的 last_update.json"

      - name: Commit and Push Changes
        if: steps.git-check.outputs.has_changes == 'true'
        working-directory: hw-list
        run: |
          git add homework_data.json last_update.json
          COMMIT_MSG="📚 自動更新家課數據（${{ steps.crawler.outputs.record_count }} 筆）- $(date +"%Y-%m-%d")"
          git commit -m "$COMMIT_MSG"
          git push

      - name: Create Summary
        if: always()
        run: |
          {
            echo "# 📊 家課資料更新報告"
            echo ""
            echo "⏱️ **執行時間：** $(date)"
            echo ""
            echo "## 🧭 更新模式"
            if [ "${{ github.event.inputs.force_full_update }}" = "true" ]; then
              echo "- 🚀 **強制完整更新**"
            else
              echo "- 📅 **日常增量更新**"
            fi
            echo ""
            echo "## 📦 更新結果"
            if [ "${{ steps.crawler.outputs.data_updated }}" = "true" ]; then
              echo "### ✅ 資料更新成功"
              echo ""
              echo "| 項目 | 數值 |"
              echo "|------|------|"
              echo "| 🗂️ 資料筆數 | **${{ steps.crawler.outputs.record_count }} 筆** |"
              echo "| 🔄 觸發方式 | \`${{ github.event_name }}\` |"
              if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
                echo "| 👤 手動觸發者 | \`${{ github.actor }}\` |"
              fi
              if [ "${{ steps.git-check.outputs.has_changes }}" = "true" ]; then
                echo "| 🚀 部署狀態 | **已自動提交更新** |"
              else
                echo "| ℹ️ 部署狀態 | 無資料變更，未提交 |"
              fi
            else
              echo "### ❌ 資料更新失敗"
              echo ""
              echo "> ⚠️ 請檢查："
              echo "> 1. GitHub Secrets 中的 PORTAL_USERNAME 和 PORTAL_PASSWORD 是否設定正確"
              echo "> 2. 網路連線是否正常"
              echo "> 3. 學校門戶網站是否可正常訪問"
            fi
          } >> $GITHUB_STEP_SUMMARY
